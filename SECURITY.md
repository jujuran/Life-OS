# Security Policy

## Supported versions

Life OS 当前处于 Alpha，仅最新 `main` 分支接受安全修复。

## 报告安全问题

请使用 GitHub 仓库的 **Security > Report a vulnerability** 私下报告。不要在公开 Issue、Discussion 或 Pull Request 中粘贴：

- API Key、访问令牌或完整请求头
- 真实账单、账户名称、SOP、产品资料或 AI 对话
- 包含本地绝对路径、用户名或身份信息的日志
- 可直接利用的漏洞细节

报告中请提供最小复现步骤、受影响版本和影响范围，并使用虚构数据。

## 当前安全边界

- Life OS 默认监听本机地址，不应直接暴露到公网。
- 本地 JSON 数据与 AI 配置依赖操作系统账户权限保护。
- AI 请求会发送到用户选择的模型服务商；发送前请确认内容不含不必要的隐私。
- 当前 API Key 可能以明文保存在本机私人数据目录。请使用受保护的系统账户和磁盘加密；未来版本计划接入系统密钥库。
- 任何远程访问、域名反向代理或 Agent 接入都必须额外配置认证、TLS 和最小权限。

## 密钥泄漏

如果密钥曾进入 Git 历史，应先在服务商处撤销并轮换，再清理历史。不要假设删除文件即可让旧密钥失效。
