# packages/agents

Agent-facing layer.

Expected contents:

- capability definitions
- input and output schemas
- risk classification
- preview-execute pipeline
- provider adapters

This package exists so replaceable external agents can work with the system safely.
