# apps/api

Backend-for-frontend and domain execution layer.

Primary responsibilities:

- auth and session context
- use case execution
- finance reporting
- sync endpoints
- audit logs
- agent approval flow

If the web app changes later, this layer should still keep the same business actions stable.
